% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_left_truncated.R
\name{score_left_truncated}
\alias{score_left_truncated}
\title{Score predictions with left truncation}
\usage{
score_left_truncated(
  risk,
  data,
  time_col,
  status_col,
  entry_col,
  landmark_col = "LM",
  id_col,
  cause = 1L,
  w,
  B = 0L,
  seed = NULL,
  conf.int = 0.95
)
}
\arguments{
\item{risk}{Numeric vector, matrix, or data frame of predicted risks. Matrix
columns are treated as models.}

\item{data}{Stacked landmark data, one row per subject and landmark.}

\item{time_col, status_col, entry_col, landmark_col}{Column names for exit,
event status, counting-process entry, and landmark.}

\item{id_col}{Subject identifier column.}

\item{cause}{Cause of interest; censoring must be coded zero.}

\item{w}{Prediction-window width.}

\item{B}{Number of subject-level bootstrap replicates. Values below two
request point estimates only.}

\item{seed}{Explicit bootstrap seed. Required when \code{B >= 2}.}

\item{conf.int}{Confidence level for bootstrap normal intervals.}
}
\value{
An object of class \code{LMScoreLT} with landmark-specific and summary
scores. Inference is marked as \code{"none"} or \code{"subject bootstrap"}.
}
\description{
Compute cumulative/dynamic AUC and cause-specific Brier scores using a
counting-process Kaplan--Meier estimate of censoring. For each subject, the
censoring probability is conditional on that subject's analysis entry.
}
\details{
Analytic influence-function standard errors are deliberately not provided:
their extension to staggered entry is not established here. If \code{B >= 2},
subject-level nonparametric bootstrap standard errors and normal intervals
are returned, with the same resample shared across landmarks.
}
